def make_iterator(iterable):
    iterator = iter(iterable) #call the __iter__ method
    return iterator


if __name__ == "__main__":
    iterable = [1,2,3,4,5,6,7,8,9]
    iterator = make_iterator(iterable)
    for i in range(len(iterable)):
        print(next(iterator)) #call the __next__ method